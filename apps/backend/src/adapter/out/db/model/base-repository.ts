import { BaseAdapter } from '~adapter/shared/base-adapter.js'

/**
 * Base repository class providing common functionality for data access implementations.
 * Extends BaseKernel for invariant checking capabilities.
 */
export abstract class BaseRepository extends BaseAdapter {}
