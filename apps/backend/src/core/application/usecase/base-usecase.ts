import { BaseKernel } from '~infra/base.js'

/**
 * Base use case class providing common functionality for business logic implementations.
 * Extends BaseKernel for invariant checking capabilities.
 */
export abstract class BaseUseCase extends BaseKernel {}
